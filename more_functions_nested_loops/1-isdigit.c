#include "main.h"

/**
 * _isdigit -  check for a digit
 * @c: digit in check
 *
 * Return:of 0 is a digit and 0 otherwise
 */

int _isdigit(int c)
{
	if
		(c >= '0' && c <= '9')
		return (1);
	else 
		return (0);
}
